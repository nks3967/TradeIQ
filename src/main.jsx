import React from 'react'
import ReactDOM from 'react-dom/client'
import TradeIQ from './TradeIQ' // Make sure this matches your file name

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TradeIQ />
  </React.StrictMode>,
)